# 🎬 GrindLab Demo Environment Setup & Usage Guide

> Руководство по запуску и использованию демонстрационной среды GrindLab

---

## ⚡ Quick Start (5 минут)

### 0a. Docker Demo (рекомендуется для презентаций)

Полностью готовый стенд без локальных зависимостей. Требуется только Docker Desktop.

**Требования:**
- Docker Desktop установлен и запущен (`docker ps` выполняется без ошибок)

**Архитектура:**
- PostgreSQL (база данных, надёжнее чем SQLite в контейнерах)
- FastAPI backend на порту 8001
- React frontend на порту 5173

**Запуск:**

```bash
# Linux/macOS
./scripts/demo-up.sh

# Windows (Powershell)
scripts\demo-up.bat

# или напрямую из корня репо
docker-compose -f docker-compose.dev.yml up --build
```

**Первый запуск (2-3 минуты):**
1. Docker собирает образы backend и frontend
2. Создаёт PostgreSQL контейнер и инициализирует БД
3. Backend стартует, создаёт таблицы, загружает demo-данные
4. Frontend собирается и стартует nginx

**Последующие запуски (10-30 сек):**
- Образы уже в кеше, используются существующие контейнеры
- Демо-данные сохранены в PostgreSQL volume

**Что открыть в браузере:**
- Frontend: http://localhost:5173
- Backend API: http://localhost:8001
- Health check: http://localhost:8001/health
- Страница проекта: проверьте графики KPI (режимы «Все расчёты» / «Базовый vs Лучший») и PSD с P80/P50; переключите версию схемы для фильтрации

**Логи (для отладки):**
```bash
# Backend logs
docker-compose -f docker-compose.dev.yml logs backend -f

# Frontend logs
docker-compose -f docker-compose.dev.yml logs frontend -f

# PostgreSQL logs
docker-compose -f docker-compose.dev.yml logs db -f

# All
docker-compose -f docker-compose.dev.yml logs -f
```

**Остановить:**
```bash
# Graceful shutdown (данные в PostgreSQL сохраняются)
Ctrl+C

# Полная остановка
docker-compose -f docker-compose.dev.yml down
```

**Очистить всё (новая БД):**
```bash
# Linux/macOS
./scripts/demo-up.sh clean

# Windows
scripts\demo-up.bat clean

# или напрямую
docker-compose -f docker-compose.dev.yml down -v
```

**Если что-то не так:**
```bash
# Пересобрать образы (без кеша)
docker-compose -f docker-compose.dev.yml up --build --force-recreate

# Полная очистка и свежий запуск
docker-compose -f docker-compose.dev.yml down -v
docker-compose -f docker-compose.dev.yml up --build
```

---

### 0. Windows one-command demo (для локальной разработки)

```powershell
# из корня репозитория
scripts\demo.ps1 -BackendPort 8001

# Скрипт сделает:
# 1) seed demo-данных (app/demo_seed.py)
# 2) поднимет backend на 127.0.0.1:8001
# 3) поднимет frontend с VITE_API_URL=http://127.0.0.1:8001
# 4) проверит /health и покажет PID-ы и логи
```

Если порт 8001 занят, укажите другой: `scripts\demo.ps1 -BackendPort 8010`.
Остановить процессы: `Stop-Process -Id <backendPid>, <frontendPid>` (PID выводится в конце).
Если порты заняты — добавить `-ForceKill`, скрипт сам освободит 5173 и указанный BackendPort.

> Требования: установленный backend venv с зависимостями (`backend/.venv`), установленные frontend-зависимости (`frontend/node_modules`).

---
### 1. Инициализация БД с демо-данными

```bash
# Перейти в backend директорию
cd backend

# Запустить seed скрипт
python app/demo_seed.py

# Вывод:
# [OK] Demo plants, flowsheets, and versions created successfully.
# [OK] Demo projects created successfully.
# [OK] Demo grind_mvp_v1 runs created successfully.
```

### 2. Запустить backend

```bash
python -m uvicorn app.main:app --host 127.0.0.1 --port 8000
# INFO:     Uvicorn running on http://127.0.0.1:8000
```

### 3. Запустить frontend (в отдельном терминале)

```bash
cd frontend
npm run dev

# Output:
# ➜  Local:   http://localhost:5173/
```

### 4. Проверить smoke-тесты

```bash
cd backend
python scripts/smoke_api.py

# Output:
# [OK] 16 smoke checks passed ✅
```

<<<<<<< HEAD
### 4a. Проверить графики и PSD (быстрый UI чек)

1. Откройте фронтенд: http://localhost:5173
2. Зайдите в проект с расчётами (например, "Тестовый проект 1")
3. Убедитесь, что на дашборде рендерятся KPI-графики
    - Переключите режим: "Все расчёты" ↔ "Базовый vs Лучший"
    - Поменяйте версию схемы — фильтр применяется к графикам
4. Откройте деталь расчёта: должны отображаться PSD-графики и значения P80/P50 (Fact vs Model)

=======
>>>>>>> origin/main
### 5. Открыть приложение

```
http://localhost:5173
```

---

## 🔍 Проверка загруженных данных

### API endpoints для проверки

```bash
# Получить список проектов
curl http://localhost:8000/api/projects/my

# Получить Dashboard проекта
curl http://localhost:8000/api/projects/1/dashboard

# Получить все расчёты
curl http://localhost:8000/api/calc-runs?limit=100

# Получить сценарии проекта
curl http://localhost:8000/api/projects/1/scenarios
```

### Ожидаемые данные в БД

```
✅ 3 Растения (Plants)
   ├─ GOLD-1 (Золотоизвлекательная фабрика №1)
   ├─ CUZN-2 (ЗИФ 2,5)
   └─ FE-3 (Железорудная фабрика №3)

✅ 5 Версий схем (FlowsheetVersions)
   ├─ gold_base_v1 (базовая SAG версия)
   ├─ gold_opt_v2 (оптимизированная версия)
   ├─ gold_sabc_v1 (SABC черновик)
   ├─ cuzn_v1 (Cu-Zn версия)
   └─ fe_v1 (Fe версия)

✅ 3 Проекта (Projects)
   ├─ Тестовый проект 1
   ├─ Оптимизация энергопотребления
   └─ Summary No Runs (для проверки пустого состояния)

✅ 9 Сценариев (CalcScenarios)
   └─ 3 сценария × 3 проекта

✅ 27 Расчётов (CalcRuns)
   └─ 3 сценария × 9 версий/версионных пар
```

---

## 📊 Презентационный сценарий

### Сценарий 1: Базовое ознакомление (10 мин)

#### Шаг 1: Home page
```
1. Откроется главная страница с 3 проектами
2. Показать что это тестовые проекты с демо-данными
3. Нажать на "Тестовый проект 1"
```

**Что показать:**
- Интерфейс управления проектами
- Переход в детали проекта

#### Шаг 2: Project Dashboard
```
Projects → "Тестовый проект 1"
```

**Что показать:**
- 2 версии схем (gold_base_v1, gold_opt_v2)
- 6 сценариев
- Список последних успешных расчётов
- Базовый сценарий: 540 т/ч, P80=184 мкм

**Рассказать:**
- "Это базовая конфигурация SAG мельницы"
- "В системе хранятся все варианты схем (версии)"
- "К каждой версии привязаны сценарии (варианты параметров)"

#### Шаг 3: Scenario Comparison
```
[Кликнуть на сценарий] → "Снижение энергии"
```

**Что показать:**
- Таблица сравнения KPI:
  - Базовый: 540 т/ч, энергия 13.3 кВт/т
  - Сценарий: 500 т/ч, энергия 12.0 кВт/т (-9%)
- Визуальное обозначение (зелёные стрелки вниз для улучшений)

**Рассказать:**
- "Система позволяет сравнивать варианты по KPI"
- "Энергия снизилась на 9%, но производительность упала на 7%"
- "Это trade-off между энергией и производительностью"

#### Шаг 4: PSD графики
```
[Скролл вниз] → "Сравнение гранулометрического состава"
```

**Что показать:**
- Два графика PSD (кумулятивный % прохода)
- Синяя линия (базовый) vs оранжевая линия (сценарий)
- Примечание: обе схемы дают P80=184 мкм (целевое значение)

**Рассказать:**
- "Несмотря на разные режимы, гранулометрический состав остаётся одинаковым"
- "Это показывает эффективность выбранной классификации"

---

### Сценарий 2: Что-если анализ (15 мин)

#### Шаг 1: Создание сценария
```
Projects → "Оптимизация энергопотребления" → [Создать сценарий]
```

**Форма создания:**
```
Название: "Максимальная энергия"
Параметры:
  - Производительность: 580 т/ч
  - P80: 180 мкм (целевое)
  - Циркуляция: 270%

[Запустить расчёт]
```

**Что происходит:**
- Система создаёт CalcScenario запись
- Запускает execute_flowsheet() для расчёта
- Сохраняет результат в CalcRun

#### Шаг 2: Batch-run (NEW в F7.2)
```
[Выбрать несколько сценариев] → [Запустить выбранные]
```

**Демонстрация:**
- ✅ Базовый сценарий
- ✅ Снижение энергии
- ✅ Рост производительности

**Результат:**
```
"Успешно запущено 3 сценариев"
✓ Все три расчёта завершены
✓ Результаты доступны для сравнения
```

#### Шаг 3: Анализ результатов
```
Dashboard → Таблица всех сценариев → Сравнение
```

**Таблица результатов:**
| Сценарий | Тех/ч | P80 мм | Энергия кВт/т | ΔЭ % |
|----------|-------|--------|---------------|------|
| Базовый | 540 | 0.184 | 13.3 | 0% |
| Энергия | 500 | 0.184 | 12.0 | -9% ✅ |
| Мощность | 580 | 0.184 | 14.0 | +5% ⚠️ |

**Вывод:**
- "Видно trade-off: улучшаем энергию (12.0), теряем мощность"
- "Система помогает принять обоснованное решение"

---

### Сценарий 3: Демонстрация пустого состояния

```
Projects → "Summary No Runs" (проект без расчётов)
```

**Что показать:**
- Информационное сообщение: "Нет расчётов, создайте первый сценарий"
- Кнопка "[Создать сценарий]"
- UI элегантно обрабатывает пустые состояния

**Рассказать:**
- "Система удобна для новых проектов"
- "Пошагово ведёт пользователя к первому расчёту"

---

## 🛠️ Advanced Usage

### Добавление своего растения

```python
# backend/app/demo_seed.py - добавить в plants_data:

plants_data = [
    {"code": "GOLD-1", "name": "Золотоизвлекательная фабрика №1"},
    {"code": "COPPER-5", "name": "Медноизвлекательная фабрика №5"},  # ← NEW
]
```

Затем:
```bash
python app/demo_seed.py
# Новое растение появится в системе
```

### Модификация параметров расчёта

**Файл**: `backend/app/demo_seed.py` → функция `_build_demo_input_json()`

```python
def _build_demo_input_json(...):
    return {
        "feed_tph": 500.0,  # ← Изменить производительность
        "ore_hardness_ab": 12.5,  # ← Изменить твёрдость
        "mill": {
            "power_installed_kw": 15000,  # ← Мощность мельницы
            ...
        }
    }
```

### Добавление новых KPI в результаты

```python
def _build_demo_result_json(...):
    return {
        "kpi": {
            "throughput_tph": 540.0,
            # Добавить новый KPI:
            "new_metric_name": 123.45,
        }
    }
```

---

## 🔄 Периодическое обслуживание

### Еженедельно

```bash
# Проверить целостность демо-данных
python scripts/smoke_api.py
```

### Ежемесячно

```bash
# Пересоздать демо-данные (очистить старые временные метки)
# 1. Удалить текущую БД
rm backend/demo.db

# 2. Пересоздать
python backend/app/demo_seed.py

# 3. Проверить
python backend/scripts/smoke_api.py
```

### Перед презентацией

```bash
# Полный чеклист
1. git checkout main
2. python app/demo_seed.py              # ✅ Свежие данные
3. python scripts/smoke_api.py          # ✅ API работает
4. npm run build                         # ✅ Frontend компилируется
5. python -m pytest tests/ -q --tb=short # ✅ Все тесты проходят
6. npm run dev                           # Запустить frontend
7. Открыть http://localhost:5173
```

---

## 📝 Логирование демо-сессии

Все действия в демо логируются:

```bash
# Просмотр логов backend
tail -f backend/logs/app.log

# Просмотр логов frontend
# Открыть Developer Console (F12) → Console tab
```

### Пример лога успешного demo-запуска

```
2025-12-25 10:00:00 INFO: Seeding demo plants...
2025-12-25 10:00:01 INFO: Demo plants created successfully.
2025-12-25 10:00:02 INFO: Seeding demo projects...
2025-12-25 10:00:03 INFO: Demo projects created successfully.
2025-12-25 10:00:04 INFO: Seeding demo runs...
2025-12-25 10:00:05 INFO: Demo grind_mvp_v1 runs created successfully.
2025-12-25 10:00:06 INFO: Demo data seeded successfully.
```

---

## 🎬 Готовые demo scripts

<<<<<<< HEAD
### Script 0: Windows PowerShell (локальная разработка)

```powershell
scripts\demo.ps1 -BackendPort 8001

# Опции:
# -NoSeed        пропустить загрузку demo данных
# -NoBackend     не запускать backend (только seed)
# -NoFrontend    не запускать frontend
# -SkipHealth    не ждать /health
# -ForceKill     принудительно освободить занятые порты (backend порт и 5173)
# -BackendPort   выбрать другой порт, если 8001 занят
```

### Script 0a: Docker (кроссплатформа, чистое окружение)
### Script 0a: Docker (кроссплатформа, чистое окружение)

```bash
# Linux/macOS
./scripts/demo-up.sh
./scripts/demo-up.sh clean   # Очистить и переинициализировать

# Windows
scripts\demo-up.bat
scripts\demo-up.bat clean

# или напрямую
docker-compose -f docker-compose.demo.yml up --build
docker-compose -f docker-compose.demo.yml down -v  # cleanup
```

---
=======
>>>>>>> origin/main
### Script 1: Full Demo Setup

```bash
#!/bin/bash
# demo-full-setup.sh

cd backend
echo "🌱 Seeding demo data..."
python app/demo_seed.py

echo "✅ Starting backend..."
python -m uvicorn app.main:app --host 127.0.0.1 --port 8000 &

cd ../frontend
echo "✅ Starting frontend..."
npm run dev &

echo "🎯 Demo environment ready!"
echo "   Backend:  http://localhost:8000"
echo "   Frontend: http://localhost:5173"
```

### Script 2: Quick Verification

```bash
#!/bin/bash
# demo-quick-verify.sh

echo "🔍 Verifying demo data..."
cd backend
python scripts/smoke_api.py

if [ $? -eq 0 ]; then
    echo "✅ Demo is ready for presentation!"
else
    echo "❌ Demo has issues. Re-run: python app/demo_seed.py"
fi
```

---

## 🆘 Troubleshooting

### Проблема: "Demo data already exists"

**Решение:**
```bash
# Опция 1: Просто пересоздать (идемпотентно)
python app/demo_seed.py

# Опция 2: Очистить БД полностью
rm backend/demo.db
python app/demo_seed.py
```

### Проблема: "Port 8000 already in use"

**Решение:**
```bash
# Найти процесс на порту 8000
lsof -i :8000

# Убить процесс
kill -9 <PID>

# Или использовать другой порт
python -m uvicorn app.main:app --port 8001
```

### Проблема: "Frontend не видит backend"

**Решение:**
```bash
# Убедиться что backend запущен
curl http://localhost:8000/health

# Если не отвечает, перезапустить backend
```

---

## 📊 Dashboard Demo Report Template

```markdown
# Demo Report: [Дата] [Клиент]

## Демонстрированные функции
- ✅ Управление проектами и версиями
- ✅ Создание и редактирование сценариев
- ✅ Запуск расчётов (single и batch)
- ✅ Сравнение результатов по KPI
- ✅ Визуализация PSD графиков
- ✅ [Другие функции...]

## Данные в демо
- Растения: 3 (GOLD-1, CUZN-2, FE-3)
- Версий схем: 5
- Проектов: 3
- Расчётов: 27

## Обратная связь
[Заполнить после презентации]

## Задачи на follow-up
- [ ] ...
```

---

**Последнее обновление: 25 декабря 2025 г.**

Вопросы? Обратитесь к документу [DEMO_DATA_BLUEPRINT.md](DEMO_DATA_BLUEPRINT.md)
