# 📐 GrindLab — Архитектура системы

**Версия:** 1.0
**Дата:** 23 декабря 2025 г.

---

## 1. Обзор

GrindLab — программный модуль для моделирования и оптимизации цепочки дробления и измельчения (comminution) на обогатительных фабриках.

### Основные возможности MVP

| Функция | Описание |
|---------|----------|
| **Конструктор схем** | Сборка технологической схемы (flowsheet) для фабрики |
| **Калибровка** | Настройка моделей оборудования по историческим данным |
| **Сценарии** | Запуск расчётов "что если?" (изменение уставок, руды) |
| **Сравнение** | Сравнение сценариев между собой и с фактом |

---

## 2. Компонентная архитектура

```
┌─────────────────────────────────────────────────────────────────────────┐
│                              КЛИЕНТ                                      │
│  ┌───────────────────────────────────────────────────────────────────┐  │
│  │                    Frontend (React + TypeScript)                   │  │
│  │  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐  │  │
│  │  │   Pages     │ │ Components  │ │  Features   │ │   API Layer │  │  │
│  │  │ (8 страниц) │ │ (reusable)  │ │ (KPI, etc)  │ │  (Axios)    │  │  │
│  │  └─────────────┘ └─────────────┘ └─────────────┘ └─────────────┘  │  │
│  └───────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    │ HTTP REST (JSON)
                                    │ Port: 5173 (dev) → 8000 (API)
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                              СЕРВЕР                                      │
│  ┌───────────────────────────────────────────────────────────────────┐  │
│  │                    Backend (FastAPI + SQLAlchemy)                  │  │
│  │                                                                    │  │
│  │  ┌─────────────────────────────────────────────────────────────┐  │  │
│  │  │                        Routers (API Layer)                   │  │  │
│  │  │  auth │ me │ projects │ flowsheets │ calc │ scenarios │ ... │  │  │
│  │  └─────────────────────────────────────────────────────────────┘  │  │
│  │                              │                                     │  │
│  │  ┌─────────────────────────────────────────────────────────────┐  │  │
│  │  │                     Services (Business Logic)                │  │  │
│  │  │         calc_service │ project_service │ run_metrics         │  │  │
│  │  └─────────────────────────────────────────────────────────────┘  │  │
│  │                              │                                     │  │
│  │  ┌─────────────────────────────────────────────────────────────┐  │  │
│  │  │                     Schemas (Pydantic DTOs)                  │  │  │
│  │  │   CalcInput │ CalcResult │ ProjectRead │ FlowsheetRead │ ...│  │  │
│  │  └─────────────────────────────────────────────────────────────┘  │  │
│  │                              │                                     │  │
│  │  ┌─────────────────────────────────────────────────────────────┐  │  │
│  │  │                     Models (SQLAlchemy ORM)                  │  │  │
│  │  │  User │ Plant │ Project │ Flowsheet │ Unit │ CalcRun │ ...  │  │  │
│  │  └─────────────────────────────────────────────────────────────┘  │  │
│  │                              │                                     │  │
│  │  ┌─────────────────────────────────────────────────────────────┐  │  │
│  │  │                          Core                                │  │  │
│  │  │        settings │ security │ rate_limit │ exceptions         │  │  │
│  │  └─────────────────────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    │ SQLAlchemy ORM
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                            DATABASE                                      │
│  ┌───────────────────────────────────────────────────────────────────┐  │
│  │              PostgreSQL (production) / SQLite (dev)                │  │
│  │                                                                    │  │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────────────┐  │  │
│  │  │   User   │ │  Plant   │ │ Project  │ │ ProjectFlowsheetVer  │  │  │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────────────────┘  │  │
│  │  ┌──────────┐ ┌──────────────────┐ ┌──────────┐ ┌──────────────┐  │  │
│  │  │Flowsheet │ │ FlowsheetVersion │ │   Unit   │ │ CalcScenario │  │  │
│  │  └──────────┘ └──────────────────┘ └──────────┘ └──────────────┘  │  │
│  │  ┌──────────┐ ┌──────────────────┐ ┌──────────┐ ┌──────────────┐  │  │
│  │  │ CalcRun  │ │  CalcComparison  │ │ Comment  │ │ UserFavorite │  │  │
│  │  └──────────┘ └──────────────────┘ └──────────┘ └──────────────┘  │  │
│  └───────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 3. Модель данных (ERD)

### Основные сущности

```
┌─────────────────┐       ┌─────────────────┐
│      User       │       │      Plant      │
├─────────────────┤       ├─────────────────┤
│ id (PK)         │       │ id (UUID, PK)   │
│ email           │       │ name            │
│ hashed_password │       │ location        │
│ is_superuser    │       │ created_at      │
│ created_at      │       └────────┬────────┘
└────────┬────────┘                │
         │                         │
         │ owner_user_id           │ plant_id
         ▼                         ▼
┌─────────────────────────────────────────────┐
│                   Project                    │
├─────────────────────────────────────────────┤
│ id (UUID, PK)                               │
│ name                                         │
│ description                                  │
│ owner_user_id (FK → User)                   │
│ plant_id (FK → Plant)                       │
│ created_at                                   │
└──────────────────────┬──────────────────────┘
                       │
         ┌─────────────┼─────────────┐
         │             │             │
         ▼             ▼             ▼
┌─────────────┐ ┌─────────────┐ ┌─────────────────┐
│CalcScenario │ │  CalcRun    │ │ CalcComparison  │
├─────────────┤ ├─────────────┤ ├─────────────────┤
│ id (UUID)   │ │ id (UUID)   │ │ id (UUID)       │
│ name        │ │ input_json  │ │ name            │
│ description │ │ result_json │ │ calc_run_ids    │
│ is_baseline │ │ status      │ │ created_at      │
│ project_id  │ │ scenario_id │ └─────────────────┘
│ fv_id       │ │ project_id  │
└─────────────┘ │ created_at  │
                └─────────────┘
```

### Схема оборудования

```
┌─────────────────┐
│    Flowsheet    │
├─────────────────┤
│ id (UUID, PK)   │
│ name            │         ┌─────────────────────┐
│ plant_id (FK)   │         │  FlowsheetVersion   │
│ created_at      │◄────────┤─────────────────────┤
└─────────────────┘         │ id (UUID, PK)       │
                            │ flowsheet_id (FK)   │
                            │ version_number      │
                            │ data (JSONB)        │
                            │ status              │
                            │ created_at          │
                            └──────────┬──────────┘
                                       │
                                       │ flowsheet_version_id
                                       ▼
                            ┌─────────────────────┐
                            │        Unit         │
                            ├─────────────────────┤
                            │ id (UUID, PK)       │
                            │ flowsheet_version_id│
                            │ unit_type           │
                            │ name                │
                            │ params (JSONB)      │
                            │ position_x, y       │
                            └─────────────────────┘
```

---

## 4. API Endpoints

### Группы API

| Префикс | Описание | Файл |
|---------|----------|------|
| `/api/auth` | Авторизация (login, register) | `routers/auth.py` |
| `/api/me` | Текущий пользователь, dashboard | `routers/me.py` |
| `/api/projects` | CRUD проектов | `routers/projects.py` |
| `/api/flowsheets` | CRUD схем | `routers/flowsheets.py` |
| `/api/flowsheet-versions` | Версии схем | `routers/flowsheet_versions.py` |
| `/api/calc` | Запуск расчётов | `routers/calc.py` |
| `/api/calc-runs` | История расчётов | `routers/calc_runs.py` |
| `/api/calc-scenarios` | Сценарии | `routers/calc_scenarios.py` |
| `/api/calc-comparisons` | Сравнения | `routers/calc_comparisons.py` |
| `/api/comments` | Комментарии | `routers/comments.py` |
| `/api/favorites` | Избранное | `routers/favorites.py` |

### Ключевые endpoints

```
POST   /api/auth/login              → JWT token
POST   /api/auth/register           → Create user

GET    /api/me/dashboard            → Dashboard data
GET    /api/me                      → Current user info

GET    /api/projects                → List projects
POST   /api/projects                → Create project
GET    /api/projects/{id}           → Get project
PUT    /api/projects/{id}           → Update project
DELETE /api/projects/{id}           → Delete project

POST   /api/calc/grind-mvp-runs     → Run GrindMVP calculation
GET    /api/calc-runs               → List calc runs
GET    /api/calc-runs/{id}          → Get calc run details

GET    /api/calc-scenarios          → List scenarios
POST   /api/calc-scenarios          → Create scenario
GET    /api/calc-comparisons        → List comparisons
POST   /api/calc-comparisons        → Create comparison
```

---

## 5. Технологический стек

### Backend

| Компонент | Технология | Версия |
|-----------|------------|--------|
| Framework | FastAPI | 0.124.0 |
| ORM | SQLAlchemy | 2.0.44 |
| Validation | Pydantic | 2.12.5 |
| Migrations | Alembic | 1.17.2 |
| Auth | python-jose (JWT) | - |
| Rate Limiting | SlowAPI | - |
| Testing | pytest | 9.0.2 |

### Frontend

| Компонент | Технология | Версия |
|-----------|------------|--------|
| Framework | React | 18.2.0 |
| Language | TypeScript | 5.3.3 |
| Bundler | Vite | 5.0.0 |
| HTTP Client | Axios | - |
| Routing | React Router | 6.21.1 |

### Database

| Среда | Технология |
|-------|------------|
| Development | SQLite |
| Production | PostgreSQL |

---

## 6. Структура директорий

```
GrindLab/
├── backend/
│   ├── app/
│   │   ├── core/              # Конфигурация, безопасность
│   │   │   ├── settings.py    # Настройки приложения
│   │   │   ├── security.py    # JWT, пароли
│   │   │   ├── rate_limit.py  # SlowAPI
│   │   │   └── exceptions.py  # Кастомные исключения
│   │   │
│   │   ├── models/            # SQLAlchemy ORM модели
│   │   │   ├── user.py
│   │   │   ├── plant.py
│   │   │   ├── project.py
│   │   │   ├── flowsheet.py
│   │   │   ├── flowsheet_version.py
│   │   │   ├── unit.py
│   │   │   ├── calc_scenario.py
│   │   │   ├── calc_run.py
│   │   │   ├── calc_comparison.py
│   │   │   └── ...
│   │   │
│   │   ├── routers/           # API endpoints
│   │   │   ├── auth.py
│   │   │   ├── me.py
│   │   │   ├── projects.py
│   │   │   ├── calc.py
│   │   │   └── ...
│   │   │
│   │   ├── schemas/           # Pydantic DTOs
│   │   │   ├── calc_io.py
│   │   │   ├── calc_result.py
│   │   │   ├── project.py
│   │   │   └── ...
│   │   │
│   │   ├── services/          # Бизнес-логика
│   │   │   ├── calc_service.py
│   │   │   ├── project_service.py
│   │   │   └── run_metrics.py
│   │   │
│   │   ├── db.py              # Database setup
│   │   └── main.py            # FastAPI app
│   │
│   ├── migrations/            # Alembic migrations
│   ├── scripts/               # Утилиты
│   └── tests/                 # pytest тесты
│
├── frontend/
│   ├── src/
│   │   ├── api/               # API client
│   │   ├── auth/              # Auth provider
│   │   ├── components/        # Reusable UI
│   │   ├── features/          # Feature modules
│   │   └── pages/             # Page components
│   └── index.html
│
├── docs/                      # Документация
├── scripts/                   # Dev scripts
└── README.md
```

---

## 7. Паттерны и принципы

### Используемые паттерны

| Паттерн | Применение |
|---------|------------|
| **Repository** | SQLAlchemy ORM абстрагирует доступ к данным |
| **DTO (Data Transfer Object)** | Pydantic schemas для API request/response |
| **Dependency Injection** | FastAPI `Depends()` для DB sessions, auth |
| **Service Layer** | Бизнес-логика вынесена в services/ |
| **Middleware** | CORS, Rate Limiting, Auth |

### Принципы

- **Separation of Concerns**: routers → services → models
- **Type Safety**: Pydantic + TypeScript
- **Immutable Data**: Pydantic frozen models где возможно
- **Fail Fast**: Валидация на входе (Pydantic), 4xx ошибки

---

## 8. Безопасность

### Текущие меры

| Мера | Статус | Описание |
|------|--------|----------|
| JWT Auth | ✅ | Токены для авторизации |
| Password Hashing | ✅ | bcrypt |
| CORS | ✅ | Настроен через env |
| Rate Limiting | ✅ | SlowAPI middleware |
| Input Validation | ✅ | Pydantic |

### TODO для production

- [ ] HTTPS only
- [ ] Secure cookie settings
- [ ] SQL injection audit
- [ ] Dependency vulnerability scan
- [ ] Secrets management (не хранить в коде)

---

## 9. Будущие интеграции

### INKA / Data Hub
- **Назначение**: Чтение исторических данных для калибровки
- **Статус**: ❓ Требуется API документация
- **Тип**: REST API / Message Queue

### Calc Service (отдельный микросервис)
- **Назначение**: Вынесение расчётного ядра
- **Статус**: 📋 Запланировано
- **Причина**: Масштабирование, изоляция тяжёлых расчётов

---

*Документ обновляется по мере развития архитектуры. Последнее обновление: 23 декабря 2025 г.*
