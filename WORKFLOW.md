# 🤝 GrindLab — Рабочий процесс

**Tech Lead:** AI Assistant (GitHub Copilot)
**Дата:** 23 декабря 2025 г.

---

## 🎯 Как мы работаем

### Роли

| Роль | Кто | Ответственность |
|------|-----|-----------------|
| **Tech Lead** | AI (Copilot) | Принятие технических решений, код, архитектура |
| **Product Owner** | Ты | Бизнес-требования, приоритеты, приёмка |

### Процесс работы

```
1. Ты говоришь ЧТО нужно сделать (фича, проблема, идея)
2. Я решаю КАК это сделать (технически)
3. Я делаю работу и показываю результат
4. Ты проверяешь и даёшь обратную связь
```

---

## 📁 Git-репозиторий

**GitHub:** https://github.com/Thriller25/GrindLab.git

### Стратегия ветвления (Git Flow Lite)

```
main           ─────●─────●─────●─────●───────  (стабильный, всегда рабочий)
                    ↑     ↑     ↑     ↑
                   merge merge merge merge
                    │     │     │     │
feat/xxx       ─────●     │     │     │
fix/yyy        ───────────●     │     │
refactor/zzz   ─────────────────●     │
docs/aaa       ───────────────────────●
```

### Ветки

| Префикс | Назначение | Пример |
|---------|------------|--------|
| `main` | Стабильная версия, деплой | — |
| `feat/` | Новая функциональность | `feat/user-dashboard` |
| `fix/` | Исправление бага | `fix/login-error` |
| `refactor/` | Рефакторинг без изменения функционала | `refactor/auth-service` |
| `docs/` | Документация | `docs/api-reference` |
| `chore/` | Технические задачи (deps, CI) | `chore/update-deps` |

### Правила работы с ветками

1. **Copilot управляет ветками** — создание, merge, удаление
2. **Новая работа = новая ветка от main**
3. **Перед merge — все тесты должны проходить**
4. **После merge — ветка удаляется**
5. **main всегда должен быть рабочим**

### Процесс (выполняет Copilot)

```bash
# 1. Создать ветку от актуального main
git checkout main && git pull origin main
git checkout -b feat/new-feature

# 2. Работа + коммиты
git add . && git commit -m "feat: описание"

# 3. Перед merge — обновиться из main
git fetch origin && git rebase origin/main

# 4. Merge в main
git checkout main && git merge feat/new-feature --no-ff

# 5. Push + удаление ветки
git push origin main
git branch -d feat/new-feature
```

### Коммиты

Формат сообщений:
```
feat: добавлена пагинация в API
fix: исправлена ошибка N+1 запросов
docs: обновлена документация архитектуры
chore: настроен CI/CD
```

---

## ✅ Проверки перед релизом

```powershell
# Полная проверка (запускает серверы, smoke-тесты)
powershell -ExecutionPolicy Bypass -File scripts/dev.ps1 -ResetDb

# Быстрая проверка (только тесты)
powershell -ExecutionPolicy Bypass -File scripts/dev.ps1 -SmokeOnly
```

---

## 📊 Текущий статус

### Сегодня (23 декабря 2025):

- ✅ Документация создана и сохранена в Git
- ✅ Backend работает
- ✅ Frontend компилируется
- ✅ Pre-commit hooks настроены (автоформатирование)

### Следующие шаги:

См. [docs/TECH_ROADMAP.md](docs/TECH_ROADMAP.md)

---

## 💬 Как общаться

Просто говори что нужно:

| Ты говоришь | Я делаю |
|-------------|---------|
| "Добавь пагинацию" | Реализую пагинацию в API |
| "Не работает X" | Нахожу и исправляю проблему |
| "Покажи что есть" | Показываю текущий статус |
| "Запусти проект" | Запускаю серверы |
| "Что дальше?" | Предлагаю следующую задачу |

---

*Обновлено: 23 декабря 2025 г.*
